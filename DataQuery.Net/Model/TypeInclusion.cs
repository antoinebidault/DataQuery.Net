namespace DataQuery.Net
{
    public enum TypeInclusion
    {
        In, // Filter on that list (AND statement)
        Out, // Exclude that list (AND statement)
        Add // Add that list to the recordset (OR statement)
    }




}
